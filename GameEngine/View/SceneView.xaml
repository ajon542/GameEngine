<UserControl x:Class="GameEngine.View.SceneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <WindowsFormsHost Grid.Column="0" Initialized="WindowsFormsHost_Initialized" SizeChanged="Resized">
        </WindowsFormsHost>
        <Slider Grid.Column="1" HorizontalAlignment="Center" Orientation="Vertical" Value="{Binding RValue}" Minimum="0" Maximum="1" Name="Red" ValueChanged="Slider_ValueChanged"/>
        <Slider Grid.Column="2" HorizontalAlignment="Center" Orientation="Vertical" Value="{Binding GValue}" Minimum="0" Maximum="1" Name="Green" ValueChanged="Slider_ValueChanged"/>
        <Slider Grid.Column="3" HorizontalAlignment="Center" Orientation="Vertical" Value="{Binding BValue}" Minimum="0" Maximum="1" Name="Blue" ValueChanged="Slider_ValueChanged"/>
    </Grid>
</UserControl>
